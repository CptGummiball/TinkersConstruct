# Files inside an otherwise-ported package that are still blocked.
#
# Both entries below are blocked on the same open decision, recorded in PORTING.md
# under "Buffer type decision": 1.21 requires RegistryFriendlyByteBuf for anything
# that serialises registry contents, but the Loadable interface passes a plain
# FriendlyByteBuf. Ingredients and item stacks are the first types that actually
# need the registry-aware buffer, so they cannot be finished until the framework's
# encode/decode signatures are widened.
#
# Fixing them by casting the buffer would compile and then fail at runtime on any
# connection that hands over a plain buffer, so they stay out until it is done properly.


# Waiting on mantle.recipe (FluidIngredient, FluidOutput), which is the next package.
slimeknights/mantle/registration/object/FluidObject.java
slimeknights/mantle/registration/object/FlowingFluidObject.java

# --- mantle.recipe stragglers, each blocked on a different piece of real work ---

# Extends Forge's AbstractIngredient. Needs Fabric's CustomIngredient API
# (fabric-recipe-api-v1): implement CustomIngredient + CustomIngredientSerializer and
# expose it through toVanilla(). Only 1 TConstruct call site, so it is not urgent.
slimeknights/mantle/recipe/ingredient/FluidContainerIngredient.java
# Base class of the two potion ingredients above; also extends AbstractIngredient.
# Zero TConstruct call sites of its own.
slimeknights/mantle/recipe/ingredient/ItemIngredient.java
slimeknights/mantle/recipe/ingredient/PotionIngredient.java
slimeknights/mantle/recipe/ingredient/PotionDisplayIngredient.java
slimeknights/mantle/recipe/helper/LoadableIngredientSerializer.java


# 1.21 deleted net.minecraft.data.recipes.FinishedRecipe; datagen now writes through
# RecipeOutput. Belongs with the datagen phase, not here.
slimeknights/mantle/recipe/helper/SimpleFinishedRecipe.java
slimeknights/mantle/recipe/helper/TagEmptyCondition.java

# 1.21 replaced RecipeSerializer's fromJson/toNetwork/fromNetwork with codec() and
# streamCodec(). Mantle's serializer helpers are the bridge between its Loadable framework
# and vanilla's recipe system, so reshaping them means deciding how a Loadable becomes a
# MapCodec + StreamCodec pair -- a design step, not a rewrite. Blocks nothing else.
slimeknights/mantle/recipe/helper/LoadableRecipeSerializer.java
slimeknights/mantle/recipe/helper/LoggingRecipeSerializer.java
slimeknights/mantle/recipe/helper/SimpleRecipeSerializer.java
slimeknights/mantle/recipe/helper/TypeAwareRecipeSerializer.java
# Same rewrite: 1.21 changed Recipe<Container> to Recipe<RecipeInput> and
# RecipeManager.byType now returns RecipeHolder rather than the recipe itself.
slimeknights/mantle/recipe/helper/RecipeHelper.java
