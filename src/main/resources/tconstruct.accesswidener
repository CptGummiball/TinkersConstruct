accessWidener v2 named
# Tinkers' Construct — Fabric access widener
#
# Converted from the Forge accesstransformer.cfg. AccessWidener requires field
# descriptors that the AT format does not carry, so entries are migrated
# alongside the module that needs them rather than in one unverifiable batch.
# See PORTING.md for the remaining AT entries still to migrate.

# Entity.wasEyeInWater — read by LivingEntityPredicate.EYES_IN_WATER.
# Migrated from the Forge AT entry for the same field.
accessible field net/minecraft/world/entity/Entity wasEyeInWater Z

# WoodType.register — Mantle registers its own wood types for signs/hanging signs.
# Vanilla made this private in 1.21; Forge exposed it via its own registration hook.
accessible method net/minecraft/world/level/block/state/properties/WoodType register (Lnet/minecraft/world/level/block/state/properties/WoodType;)Lnet/minecraft/world/level/block/state/properties/WoodType;
